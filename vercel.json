{
  "version": 2,
  "builds": [
    {
      "src": "Proyecto/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/needvox" }
    }
  ],
  "routes": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
